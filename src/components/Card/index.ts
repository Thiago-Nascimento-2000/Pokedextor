import { ButtonAbout, Container, ContainerInfo, ContainerType, Info, PokemonImage, PokemonName, Type } from "./Components";

export const Card = {
    Container: Container,
    PokemonImage: PokemonImage,
    PokemonName: PokemonName,
    ContainerType: ContainerType,
    Type: Type,
    ContainerInfo: ContainerInfo,
    Info: Info,
    ButtonAbout: ButtonAbout,
}